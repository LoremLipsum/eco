extends ./templates/template

block vars
    - var pageTitle = 'Экопарк'
    - var main = false
    - var pageId = 2

block content
    main.page-content.page-content--no-pads
        +service-intro({
        })
        +feedback({

        })
        +service-descr({
        })
        +service-bonus({

        })
        +service-garanty({

        })
        +service-teaser({

        })
