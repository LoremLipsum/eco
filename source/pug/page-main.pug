extends ./templates/template

block vars
    - var pageTitle = 'Экопарк'
    - var main = true
    - var pageId = 1

block content
    main.main-content
        .main-content__section-top-wrap
            +house-plan({
                mix: 'main-content__house-plan',
                items: house
            })
            +feedback({
                mix: 'main-content__feedback',
            })
            +house-top({
                mix: 'main-content__house-top',
                items: houseTop
            })
        +features({
        })
        +quote({})
        +map({})
        +news-diary({

        })

block video
    +modal({
        js: 'modal-video',
        video: true,
        mod: 'video',
    })
