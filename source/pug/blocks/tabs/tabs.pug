mixin tabs(data)
    +el({
        tag: 'ul',
        class: 'tabs',
        mod: data.mod,
        mix: data.mix,
        js: data.js
    })&attributes(attributes)
        each item, index in data.items
            +el({
                tag: 'li',
                class: 'tabs__item',
                mix: index == 0 ? 'active' : '',
            })
                +el({
                    tag: 'button',
                    class: 'tabs__button',
                    text: item.text,
                    js: item.js,
                })(data-tab='tab-' + (index + 1))
